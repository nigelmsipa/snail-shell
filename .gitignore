kokoro-en-v0_19/
*.opus
*.mp3
*.log
__pycache__/
*.tmp
*.zip

# --- generated read-along render data (regenerable; back up the .opus separately) ---
output/
# scratch / iteration output dirs
*_output/
frames/
wordtest/
ylt-test/
bakeoff/

# design iteration / sticky-note mockups — source of truth is ~/open-design
design/

_scratch/

# EXCEPTION: the Bella day announcements are a PAID, non-reproducible input.
# 365 ElevenLabs generations, 33 MB. Everything else in this repo can be
# remade from source; these can only be re-purchased. They are tracked
# deliberately, against the *.mp3 rule above.
!bible-in-a-year/production/announcements/*.mp3
